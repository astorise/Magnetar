# External Modules

Magnetar's Model Components, Providers, and Formats live in separate
repositories, pinned into this repository as git submodules under
`components/`, `providers/`, and `formats/`. This is a normative
requirement, not only a packaging convention -- see `project-architecture`'s
"Model Components, Providers, and Formats Are Externalized" requirement
(`externalize-runtime-extension-modules`), which also names the one
deliberate exception (a minimal, generic in-crate Reference CPU double for
the Core's own test suite) and is enforced by a CI dependency guard
(`.github/workflows/quality.yml`'s `submodule-integration` job). This
document defines versioning/release ownership per submodule
(`reach-architecture-freeze-1` task 15.3) and the current
Magnetar-to-module compatibility matrix (task 15.4).

## Modules

| Path | Repository | Status |
| --- | --- | --- |
| `components/qwen` | [Magnetar-component-Qwen](https://github.com/astorise/Magnetar-component-Qwen) | Real: implements `magnetar:model-component-graph@1.2.0`, checkpoint-configurable via the `model-config` Capability rather than fixed E2E fixture dimensions, real Qwen2/2.5 QKV attention bias support, and is the exclusive production graph source under the strict path (`reach-architecture-freeze-1` task 11.5, `implement-production-qwen-model-loading` task groups 8/12) -- see its own README |
| `components/llama` | [Magnetar-component-Llama](https://github.com/astorise/Magnetar-component-Llama) | Empty `cargo new --lib` template |
| `formats/gguf` | [Magnetar-format-GGUF](https://github.com/astorise/Magnetar-format-GGUF) | Real: parses the full GGUF container into generic `ModelTensorMetadata`, for the `F32`/`F16`/`BF16`/`I8`/`I16`/`I32`/`I64`/`F64`/`Q4_K`/`Q5_K`/`Q8_0` subset (`implement-model-format-parsers`) -- see its own README |
| `formats/safetensors` | [Magnetar-format-safetensors](https://github.com/astorise/Magnetar-format-safetensors) | Real: parses Safetensors files into generic `ModelTensorMetadata`, covering every standard Safetensors dtype (`implement-model-format-parsers`) -- see its own README |
| `providers/cpu` | [Magnetar-provider-CPU](https://github.com/astorise/Magnetar-provider-CPU) | Real: independent extraction of `ReferenceCpuExecutor`/kernels/SIMD detection (`reach-architecture-freeze-1` task group 14), the `TensorValue` structured-error-channel fix (`generalize-first-native-provider-dispatch`), and the `concat` Operator plus `resident_shape`/`copy_tensor_admitted` host-free resource primitives (`implement-device-resident-multi-step-cuda-decode`) -- see its own README |
| `providers/cuda` | [Magnetar-provider-CUDA](https://github.com/astorise/Magnetar-provider-CUDA) | Real: `CudaProvider` baseline -- device discovery, all `operator-scope` required-now kernels via NVRTC, full `ProviderExecutionApi`, passing `provider-core`/`provider-compute` conformance on real hardware (`implement-cuda-provider-baseline`), and real device-resident multi-step decode support: a device-to-device `concat` Kernel, `resident_shape`/`copy_tensor_admitted` host-free primitives, and `supports_multi_step_decode() == true` (`implement-device-resident-multi-step-cuda-decode`); real, on-device native `F16`/`bfloat16` elementwise `add`/`mul` compute, verified on real hardware against an exact reference conversion model -- not yet advertised through the Kernel Registry (`enable-native-cuda-half-precision-elementwise-compute`) -- see its own README |
| `loaders/huggingface` | [Magnetar-loader-HuggingFace](https://github.com/astorise/Magnetar-loader-HuggingFace) | Real: production Hugging Face-style Qwen bundle ingestion -- real `config.json` parsing, single-file/sharded Safetensors discovery composing `formats/safetensors`, a real `tokenizer.json`-backed tokenizer via the `tokenizers` crate (`implement-production-qwen-model-loading` task groups 2-4, 9), and a real chat-template renderer (`HuggingFaceChatTemplateFormatter`, via `minijinja`) implementing `magnetar-runtime`'s `ChatTemplateFormatter` trait (`close-tachyon-scope-audit-gaps` task group 2) -- see its own README |
| `loaders/gguf` | [Magnetar-loader-GGUF](https://github.com/astorise/Magnetar-loader-GGUF) | Real: production GGUF file ingestion for `qwen2`, `F32`/`F16`/`BF16` and quantized `Q8_0`/`Q4_K`/`Q5_K` (dequantized to `F32` before this loader's own projection-weight transpose runs) -- real architecture-config extraction from GGUF key-value metadata, tensor discovery/renaming composing `formats/gguf`, the same projection-weight transpose and tied-`lm_head` derivation `loaders/huggingface` implements for the equivalent Safetensors case, and a real byte-level-BPE tokenizer built programmatically from the GGUF file's own embedded vocabulary (`tokenizers`' `BPE::builder`, no `tokenizer.json`) (`wire-gguf-into-model-loading`, `resolve-gguf-quantized-projection-transpose-sequencing`) -- verified numerically identical to `loaders/huggingface`'s output for the same real weight values, and against the real public `Qwen2.5-0.5B-Instruct-GGUF` checkpoint in both its unquantized F16 and quantized Q8_0 exports (identical generation output for all three) -- see its own README |

## Versioning and release ownership

- **Each module versions itself independently.** A module's `Cargo.toml`
  `version` is that module repository's own concern; nothing in this
  repository requires it to track Magnetar's own version number, and a
  module's maintainer (today: whoever authors its real implementation) is
  the one who decides when to cut a release and what that release's
  version means for that module.
- **Magnetar pins, not floats.** This repository never depends on a
  module's floating branch tip; every module is a `160000` gitlink pinned
  to one exact commit (see `reach-architecture-freeze-1` task 15.1, and
  its own note about the `.gitmodules`-alone failure mode this pinning
  discipline exists to prevent). Advancing a module's pin in this
  repository is a change to *this* repository (a normal commit touching
  the gitlink), reviewed the same way any other Magnetar change is,
  independent of whether the module repository itself calls the commit
  being pinned to a "release."
- **This repository owns compatibility, not the module.** Whether a given
  module commit actually works with the current `magnetar-runtime` is
  determined by this repository's own CI (`submodule-integration` in
  `.github/workflows/quality.yml`) at the pinned commit, not by any
  version-number contract the module declares. A module bumping its own
  `Cargo.toml` version is not itself a compatibility claim.
- **No compatibility range pinning yet.** `components/llama` is still a
  template; the other five modules are real but each still has exactly one
  or two real releases, so there is nothing yet to define a meaningful
  version *range* against (a `^0.1` style constraint would be vacuous). Once
  a module has more than one real, meaningfully different release, this
  section should be extended with actual compatible-version ranges per
  module rather than the current one-pinned-commit-at-a-time model.

## Compatibility matrix

| `magnetar-runtime` | Module | Commit | Notes |
| --- | --- | --- | --- |
| This branch (`implement-production-qwen-model-loading`) | `components/qwen` | `8c75909` or later | Requires `magnetar:model-component-graph@1.2.0` (additive over `1.1.0`: `architecture-config` gained `attention-bias`; a Component compiled against `1.1.0` cannot link against a `1.2.0` host, so the checked-in artifact must be rebuilt for any change to either version); is the exclusive production graph source under the strict path (`reach-architecture-freeze-1` task 11.5). `c1e603c` makes the Component checkpoint-configurable -- architecture dimensions are queried from `model-config` at the start of every `build-prefill-graph`/`build-decode-graph` call instead of compiled-in Rust constants, and the decoder stack loops `0..num_hidden_layers` from that bound configuration (`implement-production-qwen-model-loading` task group 8), proven against a genuinely grouped-query-shaped configuration through this exact compiled binary on both Reference CPU and real CUDA hardware, not only the canonical tiny fixture dimensions. `f91290c` (carried forward unchanged) gives `rope_q`/`rope_k` a native `head_count` attribute, matching `providers/cpu`/`providers/cuda`'s own `76f082b`/`f8eb5eb`. `8c75909` adds real Qwen2/2.5 QKV attention bias support: `build_layer` emits a broadcast `add` node after each of the q/k/v projection matmuls when `config.attention-bias` is set, referencing a new `layers.N.self_attn.{q,k,v}_bias` weight edge; the v-projection's KV-cache involvement moves to the bias-add node when bias is present (the cached value must be the biased one) -- found and fixed running a real, public Qwen2.5-0.5B-Instruct checkpoint through the full production pipeline for the first time (task 12.4). `magnetar-runtime/fixtures/components/qwen-real.component.wasm` (the checked-in, compiled artifact this crate's own `#[cfg(test)]` builds and the `integration-tests/cuda-first-native` crate both use) was regenerated from this exact commit, digest updated accordingly |
| This branch | `formats/gguf` | `643bd3e` or later | Requires `magnetar-runtime`'s `model` module (`ModelTensorMetadata`, `ModelDType`, `ModelQuantization`/`ModelQuantizationFormat`, including the `GgufQ8` variant this parser's development added). `643bd3e` adds `GgufArtifact.tensor_data_start` (the tensor data section's absolute file offset, already computed internally but not previously exposed) -- needed by `loaders/gguf` to read raw tensor bytes back out of the original file without re-deriving the alignment/header-walk logic this parser already performs (`wire-gguf-into-model-loading`, which is also this parser's first real wiring into Model Loading, closing `implement-model-format-parsers`'s prior non-goal) |
| This branch | `formats/safetensors` | `7492a04` or later | Requires `magnetar-runtime`'s `model` module; not yet wired into `Model Loading` (same non-goal as above) |
| This branch | `loaders/huggingface` | `5c5c01a` or later | Requires `magnetar-runtime`'s `production_model_ingestion`/`model`/`model_format_roadmap`/`tokenizer` public contracts and `formats/safetensors`' real parser; never referenced by `magnetar-runtime` (`implement-production-qwen-model-loading` task group 1's externalization boundary, CI-guarded the same way as the other `magnetar-(component|format|provider)-*` crates). `15e695c` is this module's first real implementation: config.json/tokenizer.json/single-file and sharded Safetensors ingestion. `b786b29` normalizes real Hugging Face tensor names (`model.layers.N.self_attn.q_proj.weight`) into the canonical Model Artifact names (`layers.N.self_attn.q_proj`) the Qwen Component's `weight-edge` calls resolve directly, and rejects bias tensors structurally (the production Qwen Component graph does not yet add a bias term to any projection). `c8286aa` transposes every real projection weight (q/k/v/o_proj, gate/up/down_proj, `lm_head`) from Hugging Face's `[out_features, in_features]` storage convention to the Runtime's expected `[in_features, out_features]` logical shape -- found wiring a real ingested bundle through Model Loading for the first time (`integration-tests/production-loading`); a tiny square test config had masked this for every tensor except the ones with `hidden_size != intermediate_size`. `c355e2c` derives a tied checkpoint's `lm_head` at ingestion time when `tie_word_embeddings: true` and no `lm_head.weight` is present in the bundle (a real, common Hugging Face convention -- the model reuses `token_embedding` for the output projection): `append_synthetic_lm_head_if_tied` adds a synthetic `lm_head` tensor entry shaped from the real, discovered `token_embedding`, and `DerivedLmHeadPayloadSource` answers a read for it by transposing `token_embedding`'s real bytes, driven only by real config/tensor metadata, never a hand-built inventory (`implement-production-qwen-model-loading` task 10.5). `5c5c01a` -- found running a real, public, digest-pinned Qwen2.5-0.5B-Instruct checkpoint through this ingestor for the first time (task 12.4): (1) accepts and canonicalizes real Qwen2/2.5 `self_attn.{q,k,v}_proj.bias` tensors instead of rejecting all bias structurally, detecting the real `attention_bias` flag from the discovered tensor inventory (`config.json` never declares it); (2) the projection-transpose and derived-`lm_head` byte loops assumed F32 (4 bytes/element) unconditionally -- real Qwen2.5 checkpoints store weights as BF16 (2 bytes/element); both now derive the real per-element byte width from each tensor's own declared `storage_dtype`; (3) relaxes the tokenizer-vocabulary-size check from exact equality against `config.json`'s declared `vocab_size` to rejecting only a tokenizer vocabulary that *exceeds* it -- real checkpoints commonly pad the embedding table past the tokenizer's real vocabulary to a hardware-friendly round number (this checkpoint: 151665 real tokens, 151936 declared) |
| This branch | `loaders/gguf` | `ab72e8f` or later | Requires `magnetar-runtime`'s `production_model_ingestion`/`model`/`tokenizer` public contracts and `formats/gguf`'s real parser (`tensor_data_start` specifically); never referenced by `magnetar-runtime`. `deea1c0` is this module's first real implementation: architecture-config extraction from GGUF key-value metadata (scoped to `general.architecture == "qwen2"`), tensor discovery/renaming (GGUF's `blk.N.attn_q.weight`-style llama.cpp naming into the same canonical Model Artifact names `loaders/huggingface` produces), the same projection-weight transpose and tied-`lm_head` derivation `loaders/huggingface`'s `weight_layout.rs`/`derived_lm_head.rs` implement (ported, not shared -- these are independent externalized modules), and a real byte-level-BPE tokenizer built programmatically from the GGUF file's own embedded `tokenizer.ggml.tokens`/`merges` arrays via `tokenizers`' `BPE::builder` (no `tokenizer.json` needed). Verified two ways beyond unit tests: (1) the exact same real (non-trivial) weight values, ingested through this crate and through `loaders/huggingface` respectively, produce byte-for-byte identical generation output (`integration-tests/production-loading`'s `gguf_and_huggingface_ingestion_of_identical_weights_produce_identical_generation`) -- the strongest available proof the shape-reversal/transpose logic is correct without a real downloaded checkpoint; (2) the real public `Qwen2.5-0.5B-Instruct-GGUF` checkpoint (`qwen2.5-0.5b-instruct-fp16.gguf`, revision `9217f5d`) loads and generates the exact same output (`" 1000000"` for `"The capital of France is"`, greedy) as the already-verified `loaders/huggingface` path against the equivalent real Safetensors checkpoint. `196d28c` fixes clippy/rustfmt only (no behavior change). `d95223a` -- found running against that real checkpoint: llama.cpp's real GGUF writer always declares `tokenizer.ggml.bos_token_id`, defaulting it to the same id as `eos_token_id` for a model with no distinct BOS (true of this checkpoint); `TokenizerMetadata::validate` rejects two special tokens sharing one id regardless of kind, so registering both unconditionally made every real Qwen2 GGUF file fail tokenizer construction -- fixed by registering eos first and skipping any later kind that would claim an already-used id, matching what a Hugging Face `tokenizer_config.json`'s own omission of `bos_token` already achieves for the same real model. `3028d60` is documentation only (no code change). `ab72e8f` -- found once `support-gguf-quantized-tensor-dequantization` made real `Q8_0`/`Q4_K`/`Q5_K` dequantization available at the Model Loading layer but explicitly deferred this loader's own use of it: this loader's `weight_layout::TransposingPayloadSource` performs a real byte-level transpose of every 2D projection weight assuming a flat per-element byte width, which is meaningless for block-quantized bytes and would silently corrupt them if applied directly. Fixed by giving this crate its own ported copy of the dequantization algorithm (`src/dequantize.rs`, same convention as `weight_layout.rs`/`derived_lm_head.rs`'s pre-existing porting from `loaders/huggingface`) and dequantizing a quantized tensor's raw bytes to real `F32` at `GgufPayloadSource::read_payload` time -- before the transpose ever runs -- while overriding that tensor's discovered `storage_dtype`/`size_bytes`/`quantization` to the dequantized `F32` reality at discovery time, so every later step (transpose, tied-`lm_head` derivation, manifest construction) treats it exactly like a tensor that was never quantized. The former blanket rejection of quantized tensors is removed. Verified two ways beyond unit tests: (1) a hand-constructed, non-square, genuinely `Q8_0`-quantized projection weight dequantizes and transposes to the exact hand-computed expected values through the full `GgufIngestor::ingest` pipeline; (2) the real public `Qwen2.5-0.5B-Instruct-GGUF` checkpoint's actual `Q8_0` export (676MB, sha256-verified) loads and generates the exact same output (`" 1000000"` for `"The capital of France is"`, greedy) as its own unquantized F16 GGUF export and the Safetensors counterpart already verified -- meaningful real-world evidence for lossy quantization, not merely "does not crash" (`resolve-gguf-quantized-projection-transpose-sequencing`) |
| This branch | `providers/cpu` | `f1ac11a` or later | Requires `magnetar-runtime`'s Provider/Device/Kernel/Tensor contracts, plus (uniquely among these modules) `HostTensor`/`ReferenceCpuError`/`ReferenceCpuErrorCode` specifically, imported rather than redefined (`reach-architecture-freeze-1` task group 14); never referenced by `magnetar-runtime`. `a855224` requires `write_tensor_value`/`write_tensor_value_admitted`'s structured error channel (`generalize-first-native-provider-dispatch`) -- earlier commits do not compile against this branch's `magnetar-runtime`. `42608e1` additionally fixes `execute_invocation_with_memory_manager`'s Memory Manager admission leak (`enable-device-resident-kernel-chaining` task group 9): every Kernel dispatch previously admitted a fresh, never-released allocation for its output resource id, unbounded over a long-running session. `560895b` further makes that same function honor a caller-pre-admitted output resource (via the new `MemoryManager::admit_kernel_output`) instead of always self-admitting -- required for `unify-provider-output-admission-and-residency`'s Runtime-owned output pre-admission to actually take effect through this Provider; falls back to self-admission unchanged when no pre-admission is found. `ed9c58c` adds `publish = false` (documentation only, no code change). `76f082b` gives `rope` a native `head_count` parameter (GQA-correct multi-head rotation, `head_width = cols / head_count`, partial rotation supported, single invocation regardless of head count) -- required for `make-first-native-cuda-hot-path-device-resident`'s multi-head RoPE graph attribute; absent/`1` reproduces the pre-existing single-block behavior exactly, so this is additive, not breaking. `718bdbd` extends `add` to accept a second input of shape `[cols]`/`[1, cols]`, broadcasting it across every row of the first (real Qwen2/2.5 QKV projection bias) -- purely additive over the prior same-shape-only behavior, mirroring `rmsnorm`'s existing row-broadcast weight convention; `mul`/`residual_add` unaffected. `68cb24a` adds a `concat` Operator (row-wise concatenation, KV-history concatenation's exact contract), dispatched via a new `"concat"` match arm and advertised alongside `add`/`mul`. `0fc5724`/`f1ac11a` add `resident_shape` (structural shape metadata, no host materialization) and `copy_tensor_admitted` (duplicates a resource's bytes to a fresh identity, admitted the same way `write_tensor_admitted` already is, returning the fresh `MemoryAllocationId` a caller needs to build a `KvLayerResourceBinding` -- no Runtime-side record otherwise maps an arbitrary resource id back to it) -- together the primitives `implement-device-resident-multi-step-cuda-decode` needed to close the KV-history/pending/commit host round-trips |
| This branch | `providers/cuda` | `0cfbdf4` or later | Requires `magnetar-runtime`'s Provider/Device/Kernel/Tensor/Memory contracts and the same `write_tensor_value`/`write_tensor_value_admitted` error-channel shape as `providers/cpu` above; never referenced by `magnetar-runtime`. Its own kernel/Memory Manager/conformance behavior was verified against real GPU hardware (RTX 3070 Ti Laptop GPU), not just compiled -- see `implement-cuda-provider-baseline`, `enable-device-resident-kernel-chaining`, and `make-first-native-cuda-hot-path-device-resident`. `557ceaa` replaces host-resident storage with a real, persistent device allocation table and explicit-only host/device data movement -- `7ac13b1` and earlier commits are correct but do not satisfy the CUDA Provider baseline's own "Explicit Data Movement"/"Memory Manager Integration" spec requirements literally (device residency was claimed to the Memory Manager but not physically real). `aea1915` additionally fixes the same Memory Manager admission leak as `providers/cpu`'s `42608e1` above (identical bug pattern, task group 9). `7997a2f` further makes that same function honor a caller-pre-admitted output resource, identical to `providers/cpu`'s `560895b`, and adds `benches/kernel_chaining.rs` (measurement-only, `cargo bench` opt-in, not part of `cargo test`/CI) -- on this workstation's RTX 3070 Ti, chaining kernels via resident device buffers measured ~2.65x faster than a forced host round-trip between them, moving 5x less data across 5x fewer H2D/D2H crossings, for `unify-provider-output-admission-and-residency`. `8eb0160` closes three `docs/audits/cuda-provider-full-audit-2026-09-05.md` findings: a deterministic (real `cuMemAlloc`-failure, not hand-constructed) out-of-device-memory test, an `#[ignore]`-by-default `hardware_conformance_actually_ran_not_silently_skipped` test that `gpu-runner-smoke.yml` now runs via `--include-ignored` so a green CI run can't mean "silently skipped", and a new `deny.toml` (paired with `publish = false`) making `cudarc`'s dependency graph a real, passing `cargo deny` check for the first time. `f8eb5eb` gives the CUDA `rope` Kernel the same native `head_count` parameter as `providers/cpu`'s `76f082b` (identical GQA-correct semantics, verified to match Reference CPU within tolerance on real hardware). `8121254` adds `tests_hardware_hot_path.rs`, a real-hardware test driving weight -> MatMul -> RMSNorm -> RoPE -> a projection through the generic Kernel dispatch contract with every intermediate output staying Device-resident between steps -- `make-first-native-cuda-hot-path-device-resident`'s required end-to-end exit criterion (design.md Decision 8). `implement-production-qwen-model-loading` task 12.3/12.10's own real-hardware evidence: `integration-tests/production-loading`'s `real_production_ingestion_generates_on_real_cuda_hardware` and `hardware_conformance_actually_ran_not_silently_skipped`, run against this same RTX 3070 Ti Laptop GPU at magnetar-runtime commit `278bbdd`, both passed (5/5 in that crate with `--include-ignored`). `b851c33` adds a `bias_add_kernel` (one thread per output element, bias index `i % cols`) and dispatches to it from `CudaKernels::add` when the second input broadcasts as `[cols]`/`[1, cols]` instead of matching the first exactly, matching `providers/cpu`'s identical extension; verified against it on this same RTX 3070 Ti via a new conformance test. Also fixes two pre-existing broken rustdoc intra-doc links (`kernels.rs`'s `[CudaExecutor]`, `provider.rs`'s `[ProviderHealth::Unhealthy]`) found running `cargo doc -D warnings` for this crate for the first time this session, unrelated to the bias work. Task 12.4/12.5's real-hardware evidence, first reproduced on this workstation's RTX 3070 Ti, then independently re-verified on the `arc-gpu-magnetar` self-hosted CI runner itself (NVIDIA driver `595.71.05`, CUDA `13.3`) via `gpu-runner-smoke.yml`'s `GPU Runner Smoke Test` workflow run [`34568983611`](https://github.com/astorise/Magnetar/actions/runs/34568983611) against main repo commit `a3a34ee` (`Quality` run [`34567998597`](https://github.com/astorise/Magnetar/actions/runs/34567998597) green on the same commit) -- an external audit (`docs/audits/production-qwen-model-loading-audit-2026-09-11.md`) correctly found the change had been archived without either having actually run against its own final commit: `integration-tests/production-loading`'s `real_public_checkpoint_loads_and_generates_on_reference_cpu` and `real_public_checkpoint_prefill_output_matches_between_cpu_and_cuda` (a real, digest-pinned Qwen2.5-0.5B-Instruct checkpoint, downloaded fresh by the runner; greedy prefill token/text matching exactly between Reference CPU and this real CI GPU), both passed. `1841f32` overrides `supports_multi_step_decode` to `false` (`close-tachyon-scope-audit-gaps` task 4.2), matching this Provider's then-real device-resident-only KV-history constraint. `f768cbf` adds a device-resident `concat` Kernel (row-wise concatenation via `CudaSlice::split_at_mut` + `CudaStream::memcpy_dtod` into a fresh buffer -- no new `.cu` code needed), dispatched and advertised alongside `add`/`mul`, verified byte-identical against `providers/cpu::concat` on real hardware. `d8812d7`/`c2ff095` add `resident_shape` and `copy_tensor_admitted` (a real device-to-device copy admitted under a fresh identity, returning the fresh `MemoryAllocationId` a caller needs -- no Runtime-side record otherwise maps an arbitrary resource id back to it), verified byte-correct and non-accumulating on repeated calls on real hardware. `18c4248` removes the `1841f32` override entirely, reverting to the trait's `true` default: historical KV concatenation now dispatches through the real `concat` Kernel instead of requiring host-readable bytes this Provider never provides, so multi-step decode genuinely works. `implement-device-resident-multi-step-cuda-decode`'s own real-hardware evidence, this same RTX 3070 Ti Laptop GPU: a 4-token (later strengthened to 8-token) decode succeeds on a synthetic bundle; a 16-token decode on the real public Qwen2.5-0.5B-Instruct checkpoint matches Reference CPU's output exactly (every generated token id and the decoded text) -- the first real proof multi-step CUDA decode is not just non-crashing but numerically correct. `1303f65` adds real, on-device native half-precision (`F16`/`bfloat16`) elementwise `add`/`mul` compute (`enable-native-cuda-half-precision-elementwise-compute`, Phase 2 of `add-native-cuda-half-precision-compute`): a new `CudaHalfBuffer` type (real 2-byte device-resident bit patterns, deliberately distinct from the existing `f32`-only `CudaDeviceBuffer` -- no existing method changed) and four new kernels computing via manual IEEE 754 bit manipulation (no `cuda_fp16.h`/`cuda_bf16.h`, since NVRTC's minimal preprocessor environment does not reliably expose them), converting at the host/device boundary via this crate's own ported `half_precision` module (same algorithm as `magnetar-runtime`'s Phase 1 original). Verified on this same RTX 3070 Ti against an exact reference conversion model (decode-then-re-encode through the same round-to-nearest-even algorithm, not a tolerance band) -- all 4 correctness tests (`F16`/`bfloat16` x `add`/`mul`) passed on the first run, strong evidence the device kernel's bit manipulation matches the Rust implementation exactly. Not yet advertised through the Kernel Registry or reachable from any production graph -- see that change's `design.md` for what a future Phase 3 would need. `0cfbdf4` is documentation only (no code change) |

`components/llama` has no real content yet, so no compatibility claim is
meaningful for it beyond "the empty template builds" (verified by
`submodule-integration`). This table should gain a row for it once it has
real content to be compatible (or not) with.
