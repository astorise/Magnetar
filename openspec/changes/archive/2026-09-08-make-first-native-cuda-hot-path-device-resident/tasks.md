## 1. ResourceAffinity: resolved helper, Resident preservation, production validation (design.md Decision 6)

- [x] 1.1 Add `resolved_resource_affinity(prepared_plan, node, execution_context) -> ResourceAffinity` to `first_native_runtime.rs`, mirroring `resolved_output_placement`'s lookup exactly.
- [x] 1.2 `dispatch_reference_cpu_operator_multi` calls it for `NodeInputResource::Fresh` resources and its own new outputs (replacing the hardcoded `ProviderBinding::new(REFERENCE_CPU_PROVIDER_NAME)`).
- [x] 1.3 For `NodeInputResource::Resident` resources, look up the resource's existing recorded `ResourceAffinity` (via its `TensorResidency`/Memory Manager record) and aggregate it with the dispatch's own affinity through the existing `resource-affinity` aggregation contract, rejecting on conflict rather than overwriting. Extracted as `resident_resource_affinity` for direct testability.
- [x] 1.4 Added the production-level (not `debug_assert!`) structured validation at the Kernel/Runtime dispatch boundary, extracted as `validate_invocation_provider_matches_affinity` (also for direct testability): after `KernelDispatchPlan` is built, rejects with `InferenceApiError::GraphPlanningFailed` if `invocation.kernel.provider` disagrees with the affinity's own provider. Real, not hypothetical: `PlanNodeBinding::new` accepts its `kernel: KernelId` and its own `provider: ProviderBinding` as independent parameters with no validation they agree (confirmed by reading it).
- [x] 1.5 Test: `resolved_resource_affinity_matches_a_non_reference_cpu_plan_binding` / `..._falls_back_to_reference_cpu_without_a_binding`.
- [x] 1.6 Test: `resident_resource_affinity_is_preserved_not_overwritten` (a Device binding absent from the dispatch's own affinity survives aggregation) / `resident_resource_affinity_conflict_is_rejected`.
- [x] 1.7 Test: `validate_invocation_provider_matches_affinity_rejects_a_real_divergence` / `..._accepts_agreement`.
- [x] 1.8 Full regression: 1201/1201 `magnetar-runtime` tests pass (1195 + 6 new), clippy/fmt clean.

## 2. RMSNorm Host materialization removed (design.md Decision 4)

- [x] 2.1 Confirmed: `providers/cpu::rmsnorm`/`providers/cuda::CudaKernels::rmsnorm` both derive `cols` from the input's own shape and accept a `[cols]`-shaped weight, broadcasting internally.
- [x] 2.2 `dispatch_qwen_rmsnorm`'s signature changed `input: HostTensor, weight: HostTensor` to `input: NodeValue, weight: NodeValue`; manual per-row weight broadcast deleted.
- [x] 2.3 `dispatch_qwen_graph_node`'s "rmsnorm" arm stops calling `.into_host()` on either input before dispatch.
- [x] 2.4 Updated all 6 call sites across the two large `#[cfg(test)]` oracle functions for the new signature; one of them (`post_attention_host`) was itself an unneeded `.into_host()` this fix removed outright.
- [x] 2.5 Test: `rmsnorm_accepts_a_resident_input_without_materializing_it_first` -- a MatMul-shaped output already Resident under `OpaqueReportingExecutor` passes into RMSNorm and computes the correct real result (RMS-normalization of `[2,4,4,8]` -> `[0.4,0.8,0.8,1.6]`), previously impossible to even call with a Resident value.
- [x] 2.6 Full regression: 1202/1202 `magnetar-runtime` tests pass (1201 + 1 new), including every existing Reference CPU forward-pass test unchanged; clippy/fmt clean.

## 3. RoPE: explicit `head_count` graph attribute, corrected multi-head semantics (design.md Decisions 1-2)

- [x] 3.1 `operator.rs`'s `"rope"` `OperatorAttributeSchema` gains `head_count` as `OperatorAttributeRule::optional(OperatorAttributeKind::Integer)`.
- [x] 3.2 Schema test: `rope_schema_accepts_head_count_with_correct_kind_only` -- absent/`1`/`>1` accepted, wrong kind rejected, a still-unknown attribute name still rejected.
- [x] 3.3 `qwen_model_component.rs`'s `rope_q` node construction adds `head_count = a.attention_head_count`; `rope_k` adds `head_count = a.kv_head_count`.
- [x] 3.4 `providers/cpu::rope` gains `head_count: u64` with the corrected `head_width = cols / head_count` semantics and copy-seeded output.
- [x] 3.5 `providers/cpu` tests: regression (`head_count` absent/`1`) plus new multi-head, partial-RoPE, and GQA-shaped cases, each cross-checked against independent single-head slices.
- [x] 3.6 `providers/cuda/src/kernels.cu`'s `rope_kernel` gains `head_count`, corrected `(row, head, pair)` indexing, device-to-device copy seed (`clone_dtod`, replacing `alloc_zeros`).
- [x] 3.7 `providers/cuda/src/kernels.rs`'s `CudaKernels::rope` gains the matching parameter, updated launch config (`rows * head_count * half`), and the same validation as 3.4.
- [x] 3.8 `providers/cuda` tests (real hardware, RTX 3070 Ti): `rope_matches_reference_cpu` (unchanged), plus new `rope_multi_head_matches_reference_cpu`/`rope_partial_rotation_matches_reference_cpu`/`rope_gqa_shaped_head_count_matches_reference_cpu` -- all pass against `providers/cpu`'s corrected implementation.
- [x] 3.9 `CudaExecutor`'s and `ReferenceCpuExecutor`'s (`providers/cpu`) "rope" dispatch arms read `head_count`, default `1`.
- [x] 3.10 `first_native_runtime.rs`: RoPE dispatch reads `head_count` via `node.attributes.get("head_count")`, falling back to `qwen_rope_head_count(node, architecture)` only when absent.
- [x] 3.11 Deleted `dispatch_qwen_rope_per_head`; replaced with single-dispatch `dispatch_qwen_rope`. `output_target` deliberately stays `None` for this arm (matches the pre-existing, unchanged top-of-function comment: RoPE's KV-cache-tracked output edge is handled by `execute_qwen_graph_nodes`'s own unconditional path for this node kind, not pre-admission).
- [x] 3.12 Updated all 6 oracle call sites (4 rope calls + 2 downstream materializations) across the two large `#[cfg(test)]` oracle functions.
- [x] 3.13 Re-verified: the full 1208-test suite includes the per-node causal-evidence chain tests unchanged.
- [x] 3.14 Full regression, with a genuine bug found and fixed along the way (see below): `magnetar-runtime` 1208/1208, `providers/cpu` (own suite), `providers/cuda` 27/27 real hardware, clippy/fmt/wasm32 clean.

**Critical bug found and fixed during 3.14's regression pass, not by inspection but by a real end-to-end oracle divergence**: `magnetar-runtime/src/reference_cpu.rs` carries its own in-crate copy of `rope()` (the "in-crate test double" the module's own doc comment describes, independent from `providers/cpu`'s copy) -- and its "rope" Kernel dispatch arm is what `ReferenceCpuExecutor` (the executor `dispatch_qwen_graph_node` and every oracle actually dispatch through) runs. Tasks 3.4-3.9 above updated `providers/cpu` and `providers/cuda`'s copies and dispatch arms, but missed this third copy entirely -- it kept its old 5-argument signature and silently ignored the new `head_count` attribute, rotating only the first `dimension` columns of every row regardless of `head_count`, leaving every head beyond the first completely unrotated. This produced identical (but wrong) results whether compared through `dispatch_qwen_graph_node` (the graph) or through the oracle functions this task group also updated to call the same shared `dispatch_qwen_rope` -- both shared the same silent bug, so they agreed with each other while disagreeing with `e2e_forward_hidden_states`'s fully independent, hand-written reference implementation (`apply_rope_per_head`, deliberately left untouched), which is what caught it. Fixed by applying the identical `head_count`/`head_width` correction to `reference_cpu.rs`'s own `rope()` and its dispatch arm, plus the same new multi-head/partial/GQA test trio in `reference_cpu/tests.rs`. This is the exact class of gap task group 7's end-to-end hardware test exists to catch -- confirmed here at the Reference CPU level before ever reaching real CUDA hardware.

## 4. Weight edges thread `NodeValue`, shaped from `TensorEdge.descriptor` (design.md Decision 3)

- [x] 4.1 `resolve_qwen_weight_edge`'s return type changed `HostTensor` -> `NodeValue`; resolves the weight edge's `TensorDescriptor` from the graph's own `TensorEdge.descriptor` (new parameter), not the Provider, not Qwen config. `provider.read_tensor_value(resource_id)` maps `Host` -> `NodeValue::Host`, `Opaque` -> `NodeValue::Resident { id, shape: descriptor.shape.dimensions.clone() }`. `lm_head`'s tied-embeddings branch still materializes for its Rust-side transpose (task group 5 removes this).
- [x] 4.2 Updated the one call site (`execute_qwen_graph_nodes`'s weight-edge resolution, which now also looks up the edge for its descriptor). Also fixed a related gap found in the same area: `passthrough_eligible`'s list still excluded `rmsnorm`/`rope` even though task groups 2-3 already made both `NodeValue`-based -- added them, so a Resident edge feeding either no longer gets forced to materialize one level up before ever reaching their own (already-fixed) dispatch functions.
- [x] 4.3 Test: `weight_edge_resolves_opaque_weight_to_resident_without_materializing` -- an `Opaque` weight resolves to `NodeValue::Resident` with the graph edge's shape, not an error.
- [x] 4.4 Full regression: 1209/1209 `magnetar-runtime` tests pass (1208 + 1 new), clippy/fmt clean.

## 5. `lm_head` tied embeddings: transpose once at Model Load (design.md Decision 5)

- [x] 5.1 New shared helper `qwen_weights_with_derived_lm_head` (not inside `WeightMaterializationTransaction` itself, which stays generic per Correctif 9): for a tied-embeddings fixture with no explicit `lm_head` entry, transposes `token_embedding` once and inserts the result under the name `lm_head`, before the weight map ever reaches `materialize_model_instance_weights`. Called from `bind_qwen_fixture_weights` and `load_fixture_instance_with_weights` (both weight-loading entry points this fixture has).
- [x] 5.2 `resolve_qwen_weight_edge` no longer has *any* `lm_head`/`tied_embeddings` special case -- its `tied_embeddings: bool` parameter was removed entirely; `lm_head` now resolves exactly like any other weight, by name, already pre-transposed. `weight_bindings` genuinely contains an `lm_head` entry from Model Load onward.
- [x] 5.3 Test: `lm_head_weight_is_transposed_once_at_model_load_not_per_generation_step` -- runs a real prefill then decode dispatch against the same `ModelInstance`, confirms the `lm_head` resource id and its staged data (byte-identical to `token_embedding`'s independently-computed transpose) are unchanged after both dispatches.
- [x] 5.4 Full regression: 1210/1210 `magnetar-runtime` tests pass (1209 + 1 new), including the existing weight-sensitivity/tied- and non-tied-embeddings tests, unchanged; clippy/fmt clean.

## 6. Pre-admission rollback: a real transactional guarantee (design.md Decision 7)

- [x] 6.1 Red test baseline: before this fix, `resolve_output_target` admitted a caller's output unconditionally via `admit_kernel_output`, and neither it nor any of the leaf `dispatch_qwen_*` functions released that admission if the subsequent `dispatch_reference_cpu_operator` call failed for any reason -- confirmed by reading every call site (`resolve_output_target`'s own doc comment, pre-fix, documented no rollback of its own). Fix: new `rollback_pre_admitted_output` (releases the `TensorResidency` and its Memory Manager allocation, best-effort) plus a new consolidated wrapper `dispatch_reference_cpu_operator_pre_admitted` (pre-admits via `resolve_output_target`, dispatches, rolls back on any `Err`) replacing the old `resolve_output_target` + `dispatch_reference_cpu_operator` two-step pattern at all 7 call sites (`dispatch_qwen_matmul`, `dispatch_qwen_unary`, `dispatch_qwen_binary_same_shape`, `dispatch_qwen_rmsnorm`, `dispatch_qwen_rope`, `dispatch_qwen_attention`, and the "embedding" match arm inline in `dispatch_qwen_graph_node`).
- [x] 6.2 Rollback on submit-time failure: new test `e2e_graph_dispatch_rolls_back_pre_admitted_output_on_submit_time_failure` reuses the existing tight-Runtime-memory-budget scenario (`attention`'s required workspace failing to admit -- a genuine Kernel Registry/dispatch-plan-construction-adjacent failure, since workspace allocation never even creates an allocation object on failure) and additionally asserts the failing node's own pre-admitted output allocation (the highest-id allocation at that point, mirroring `stage_weight_propagates_and_rolls_back_on_provider_write_failure`'s existing reasoning) ends in `Released` state, not leaked as `Active`.
- [x] 6.3 Rollback on kernel/completion-time failure: new mock `KernelFailableProviderExecutionApi` (wraps a real `ReferenceCpuExecutor`, injects a `complete_kernel` failure for one targeted Operator by name, delegates everything else including weight materialization untouched) plus `TestFailableKernelProvider` (a full `Provider` -- `devices()`/`kernel_advertisements()` included, unlike the narrower `TestFailableProvider` -- needed since this test drives real Kernel-Registry-backed graph dispatch, not just weight materialization). New test `e2e_graph_dispatch_rolls_back_pre_admitted_output_on_kernel_completion_failure` targets `embedding` (the first node this fixture's prefill graph dispatches, so zero earlier nodes have admitted anything of their own) and asserts the Runtime's Active allocation count after the failed dispatch is exactly one higher than right after model loading -- the "+1" being the graph's own `input.token_ids` resource, admitted up front before any node dispatches and legitimately never rolled back.
- [x] 6.4 Both Memory Manager and Provider storage are exercised by 6.2/6.3: 6.2's assertion reads `MemoryManager::allocations()` state directly; 6.3's mock additionally proves the Provider-side handle bookkeeping (`failing_handles`) never falsely believes a rolled-back dispatch's Kernel work is still outstanding -- a cleared `fail_operator`/`failing_handles` retry (see 6.5) would itself fail if Provider-side state were left inconsistent by the rollback.
- [x] 6.5 Final test: 6.3's test additionally retries the identical dispatch (fresh `KvCacheId`, injected failure cleared) immediately after the rolled-back failure and asserts it now succeeds cleanly -- proving no orphaned allocation or stale `TensorResidency` under the same resource id survives to collide with or corrupt a subsequent legitimate dispatch.
- [x] 6.6 Full regression: 1212/1212 `magnetar-runtime` tests pass (1210 + 2 new), clippy `-D warnings` clean, `cargo fmt --check` clean.

## 7. Real CUDA end-to-end proof (design.md Decision 8) -- required exit criterion

- [x] 7.1 New `providers/cuda/src/tests_hardware_hot_path.rs`. Qwen's own dispatch functions (`magnetar_runtime::first_native_runtime`'s `dispatch_qwen_*`, `execute_qwen_graph`, the `E2eFixture`/Model Instance/Component machinery) are private to `magnetar-runtime` and unreachable from `providers/cuda` (a downstream crate) -- confirmed by reading `magnetar-runtime/src/lib.rs`'s public surface end to end. What *is* public, and what this test uses instead, is the generic Kernel dispatch contract those private functions are themselves built from: `Runtime::builder()`, `KernelSelectionRequest`, `KernelRegistry::select`, `KernelDispatchPlan::from_selection`, `KernelDispatcher::revalidate`, `initial_operator_catalog`, and `ProviderExecutionApi::submit_kernel`/`complete_kernel`/`write_tensor_value_admitted`/`read_tensor`/`read_tensor_value` -- registering a real `CudaProvider` and driving it through exactly the same sequence `dispatch_reference_cpu_operator_multi` uses internally. Skips cleanly (returns without assertions) when no compatible CUDA driver/device is present, matching every other hardware-gated test in this crate.
- [x] 7.2 `stage_weight` helper writes each of 4 small tensors (one activation, three weights) Device-resident via `write_tensor_value_admitted`, the same admit-then-write path `WeightMaterializationTransaction::stage_weight` uses for a real Model Instance's weights.
- [x] 7.3 `weight_matmul_rmsnorm_rope_projection_chain_runs_device_resident_on_real_hardware` dispatches the full chain on real hardware (this workstation's RTX 3070 Ti): weight -> MatMul ([2,4]x[4,4]) -> RMSNorm ([4] weight) -> RoPE (`dimension=4`, `head_count` absent/1) -> a projection (MatMul again, [2,4]x[4,3] -- the same Kernel this Runtime uses for every linear projection, `lm_head` included).
- [x] 7.4 All asserted: every intermediate step's `read_tensor_value` returns `TensorValue::Opaque` (never `ResidencyUnavailable`) and is reused by resource id directly as the next step's input -- no download/reupload between MatMul/RMSNorm/RoPE/the projection, only one explicit `read_tensor` download at the very end; each produced resource's `TensorResidency.affinity.provider()`/`.placement` checked directly against the Memory Manager's own ledger and confirmed CUDA/GPU0 throughout (task group 1's `validate_invocation_provider_matches_affinity`, exercised on every one of these real dispatches, never had anything to reject); the downloaded final result matches an independently-computed Reference CPU oracle (`magnetar_provider_cpu::matmul`/`rmsnorm`/`rope` chained the same way) within `1e-3`; exactly 8 allocations (4 staged + 4 produced) are `Active` afterward, none leaked.
- [x] 7.5 Scaffolding stayed within one new ~350-line test file reusing only existing public primitives -- no new production code, no changes to `magnetar-runtime`'s public surface, nothing to report back per design.md's Risk note.
- [x] 7.6 Full regression: `providers/cuda` 28/28 tests pass (27 + 1 new; 1 correctly `#[ignore]`d), clippy `-D warnings` clean, `cargo fmt --check` clean.

## 8. OpenSpec accuracy pass (audit P1-3)

- [x] 8.1 Read `implement-cuda-provider-baseline/tasks.md` fully (all 11 task groups); found and corrected two stale completion notes still describing pre-`enable-device-resident-kernel-chaining` behavior -- task 7.1 (claimed bytes round-trip host↔device *within* each `CudaKernels` call rather than staying resident *between* calls; `executor.rs`'s module doc is now literally titled "Storage is device-resident between calls") and task 6.1 (claimed every `CudaKernels` kernel method uploads its own inputs/downloads its own output per call; those methods now take/return `CudaDeviceBuffer` directly with no upload/download of their own). Also found and fixed one genuinely stale checkbox unrelated to that behavior change: task 7.3 (the out-of-device-memory unit test) was actually implemented (`tests.rs`'s `out_of_device_memory_is_triggered_by_a_real_over_capacity_allocation`) but left unchecked.
- [x] 8.2 Confirmed this change's own spec deltas (`operator`, `operator-scope`, `cuda-provider`, `resource-affinity`) match what shipped in tasks 1-7: `head_count` attribute semantics (operator, operator-scope, cuda-provider) match `qwen_model_component.rs`/`kernels.cu`/`kernels.rs`'s actual implementation and are covered by the new `rope_gqa_shaped_head_count_matches_reference_cpu`/`rope_partial_rotation_matches_reference_cpu` tests; production-build `ResourceAffinity` validation and Resident-resource-affinity-preservation (resource-affinity) match `validate_invocation_provider_matches_affinity`/`resident_resource_affinity` and their task group 1/6 tests. No corrections needed.

## 9. Cross-repository sequencing and full verification

- [x] 9.1 `cargo build -p magnetar-runtime --lib` clean; `cargo test -p magnetar-runtime --lib` 1212/1212 passing; `cargo clippy -p magnetar-runtime --lib --tests -- -D warnings` clean; `cargo fmt --check` clean; `cargo build --workspace` clean.
- [x] 9.2 `cargo check --target wasm32-unknown-unknown -p magnetar-runtime --all-features` exits 0 (pre-existing warnings only, in the no-strict-Component-engine wasm32 fallback path this change never touched -- not a regression).
- [x] 9.3 `providers/cpu`: 14/14 tests pass, clippy/fmt clean. `providers/cuda`: 28/28 tests pass (1 correctly `#[ignore]`d), clippy/fmt clean, `cargo deny --manifest-path providers/cuda/Cargo.toml check` exits 0 (`advisories ok, bans ok, licenses ok, sources ok`).
- [x] 9.4 `providers/cpu` had no changes this task group (already pinned at `76f082b` from task group 3); `providers/cuda` committed and pushed (`8121254`); parent committed and pushed (`9cdfb16`), with `SUBMODULES.md`'s compatibility matrix updated for both modules' commits.
- [x] 9.5 Dispatched `gpu-runner-smoke.yml` on `main` after all of the above landed: run `34193381867`, green (`Build and test the CUDA Provider` passing) on `arc-gpu-magnetar`'s real GPU, exercising `providers/cuda@8121254` -- the exact commit carrying task group 7's new hot-path test.
- [x] 9.6 `openspec validate make-first-native-cuda-hot-path-device-resident --strict`: "Change 'make-first-native-cuda-hot-path-device-resident' is valid".

## 10. Second audit response (`audit-complet-cuda-hot-path-2026-09-08`, NO-GO -- 2 P0, 2 P1, 3 P2)

- [x] 10.1 P0-1 fixed: `ResourceAffinity` validation moved to the generic `KernelDispatcher::revalidate_with_context` boundary (`kernel_dispatch.rs`) -- every dispatch passes through this regardless of caller, not a private first-native-only helper. Now checks Provider **and** Device (the prior check was Provider-only, and could not have caught the same Provider resolving to the wrong Device among several it exposes) and covers both `input_bindings` and `output_bindings` (the prior check only walked outputs). The superseded private `validate_invocation_provider_matches_affinity` (and its call site) was removed from `first_native_runtime.rs` -- `dispatcher.revalidate(...)`, already called on every dispatch a few lines later, now performs a strict superset of what it checked. 6 new tests in `magnetar-runtime/src/tests.rs` (`kernel_dispatcher_revalidate_*`) cover the audit's own test list exactly: provider mismatch, device mismatch with the same Provider (the specific regression the old check could not catch), input resource mismatch, output resource mismatch, prepared-plan device mismatch, and a positive "fully consistent plan" baseline.
- [x] 10.2 P0-2 fixed: `rollback_pre_admitted_output` now calls `provider.release_tensor(id)` (idempotent: `Ok(false)` when nothing was materialized, e.g. a submit-time failure) *before* releasing the Memory Manager side, closing the "Provider storage keeps an orphaned tensor after a rolled-back completion-time failure" gap -- confirmed as a genuine bug (not theoretical) by first proving the exact scenario the audit described: Reference CPU's `submit_kernel_invocation` runs the Kernel and writes its output into Provider storage *synchronously*, before `complete_kernel` is ever called, so a completion-time failure really does leave a materialized-but-now-orphaned Provider-side resource without this fix. Both existing task-group-6 rollback tests extended: the kernel-completion-failure test now asserts `executor.read_tensor_value(embedding_output_id).is_none()` after rollback (proven red without the fix, confirmed green with it); the submit-time-failure test asserts the equivalent for the never-materialized case.
- [x] 10.3 P1-2 fully closed (superseding the earlier "investigated, not implemented" note below, kept for its own record): `components/qwen`'s `rope_attrs` now sets `head_count` explicitly (Q = `ATTENTION_HEAD_COUNT`, K = `KV_HEAD_COUNT`, `components/qwen@f91290c`), `magnetar-runtime/fixtures/components/qwen-real.component.wasm` was rebuilt from that source (`cargo build --target wasm32-unknown-unknown --release` + `wasm-tools component new`) and its digest updated in both the checked-in manifest and `QWEN_REAL_COMPONENT_DIGEST`, and `qwen_rope_head_count`'s node-id-suffix fallback was then removed from `first_native_runtime.rs` -- confirmed genuinely dead this time: the full suite passes without it, against the *regenerated* real Component, not the Rust-side graph builder. First attempt (documented below) correctly found this unsafe against the *stale* Component; the real fix was regenerating the artifact, not skipping the removal.
  - Original note, preserved: "P1-2 investigated, **not** implemented as literally requested -- the audit's Correctif D... turned out to be unsafe... That binary genuinely does not emit `head_count`... The fallback is retained, with the real prerequisite documented in code: regenerating `qwen-real.component.wasm`..." -- that prerequisite is what 10.3 above now does.
- [x] 10.4 P2-2 fixed: `CudaKernels::rope`'s launch-size arithmetic (`rows * head_count * half`) extracted to a standalone `rope_launch_work_items` function using `checked_mul` plus `u32::try_from`, replacing the previous bare `as u32` cast that could silently overflow/truncate for large enough shapes. 3 new hardware-independent unit tests (`kernels::rope_launch_work_items_tests`) exercise the checked path directly, including two genuine overflow cases no real fixture is large enough to trigger.
- [x] 10.5 Full regression after 10.1-10.4: `magnetar-runtime` 1218/1218 tests pass, clippy `-D warnings` clean, `cargo fmt --check` clean, `cargo build --workspace` clean, wasm32 check clean; `providers/cuda` 31/31 tests pass (1 correctly `#[ignore]`d, including the real-hardware hot-path test), clippy/fmt clean.
- [x] 10.6 P1-1 fully closed: a real first-native CUDA E2E, not just generic Kernel dispatch through CUDA, verified on real hardware. Two parts, both required:
  - **Generalized the first-native entrypoint, zero CUDA code in `magnetar-runtime`**: new `build_runtime_with_model_execution_engine_and_provider`/`register_prepared_kernels_for_provider`/`load_fixture_instance_for_provider`/`load_fixture_instance_with_weights_for_provider`/`prepare_first_native_execution_plans_for_provider` (and `prepare_first_native_plan_for_graph` gaining a `provider: &ProviderBinding` parameter, replacing two more hardcoded `REFERENCE_CPU_PROVIDER_NAME` reads -- the actual reason every Prepared Plan bound to Reference CPU regardless of what was registered) all resolve entirely through the `Provider`/`Device` trait objects this crate already depends on. New public `run_first_native_graph_with_provider`/`run_first_native_graph_with_provider_and_weights` tie them together, returning the live `Runtime` (not torn down) for direct residency inspection. Also made public: `FirstNativeComponentGraphs` and `build_first_native_graphs_from_real_qwen_component` (the real Component invocation production itself uses -- exposing it doesn't weaken "production never Rust-synthesizes a graph", it *is* the production path either way) and `e2e_fixture_manifest_from_weights` (computes per-tensor digests from an arbitrary weight map instead of the checked-in Safetensors bytes, letting a caller build a fixture for a `QwenConfig` other than the one canonical one). `qwen_build_graph`/`qwen_prefill_graph`/`qwen_decode_graph` were deliberately **not** touched -- `magnetar-runtime/Cargo.toml`'s own comment documents removing an equivalent production fallback path on purpose; reusing them externally, even just for verification, would have reversed that decision without being asked to.
  - **New crate `integration-tests/cuda-first-native`** (main repo, not a submodule -- test-only glue, not a Provider/Component/Format implementation, so `externalize-runtime-extension-modules` does not apply), depending on both `magnetar-runtime` and `magnetar-provider-cuda`. Two tests, both green on real hardware (RTX 3070 Ti): `real_qwen_model_instance_dispatches_on_cuda_device_resident_and_matches_reference_cpu` (the real, checked-in Component's graph, real `ModelInstance`, real Prepared Plan bound to CUDA, real weight materialization, Device residency and no-Reference-CPU-fallback checked directly against the Memory Manager's own ledger, numeric parity vs. an identical Reference CPU run, no allocation leak) and `genuinely_gqa_shaped_rope_dispatches_on_cuda_device_resident_with_distinct_head_counts` (`attention_head_count=4, kv_head_count=2`, a graph hand-built from `magnetar-runtime`'s always-public `ExecutionGraph`/`ExecutionNode`/`TensorEdge` primitives -- not `qwen_build_graph` -- since the real Component's architecture is hard-coded and can never be GQA-shaped; same residency/no-fallback/numeric-parity checks).
  - **Two real, previously-unknown production bugs found and fixed along the way** (both real Reference-CPU-only blind spots -- neither was ever reachable before `run_first_native_graph_with_provider` made a non-Reference-CPU Prepared Plan possible at all): (1) `kernel_memory_class_for_edge` (used by `prepare_first_native_plan_for_graph`'s Kernel Registry selection) mapped every graph edge's default, unconstrained residency to `KernelMemoryClass::Host` unconditionally -- correct by coincidence for Reference CPU's own Host-only kernels, but rejected outright (`MemoryClassUnsupported`) for CUDA's Device-only kernels; now resolves the default case from the target `ProviderBinding` (Reference CPU -> Host, else -> Device), leaving an edge's own *explicit* residency constraint (`Device`/`BrowserLinearMemory`/`ProviderOwnedOpaque`) untouched. (2) `execute_qwen_graph_nodes`'s final "materialize every remaining live edge back to `HostTensor`" boundary used `read_tensor_value(...).into_host(...)`, which is structurally incapable of ever downloading a genuinely Device-resident value (`read_tensor_value` is documented to always answer `Opaque` for such a Provider, by design) -- extracted into its own function, `materialize_qwen_graph_bindings` (deliberately outside `execute_qwen_graph_nodes`'s own body, so the existing static guard against raw `HostTensor`-typed calls in that function's *per-node transport* keeps its exact original scope and meaning), using the real `read_tensor` download instead.
  - Full regression after both fixes: `magnetar-runtime` 1218/1218 (including the pre-existing static-transport-guard test, unchanged), clippy/fmt clean; `providers/cuda` unaffected, still 31/31; `integration-tests/cuda-first-native` 2/2 on real hardware, clippy/fmt clean.
- [x] 10.7 P2-1 (`resolved_kernel_memory_class`/`resolved_output_placement`/`kernel_memory_class_for_edge` deriving Host-vs-Device from "is this Reference CPU" rather than contractual `KernelAdvertisement`/Provider capability data) and P2-3 (CUDA hardware conformance profile / non-vacuous `ProviderDataMovement` follow-up not yet a named, dedicated OpenSpec change) -- left open, explicitly recorded here per the audit's own acceptance criterion for deferring P2s ("à condition d'être explicitement enregistrés"). `kernel_memory_class_for_edge` gained one more instance of the same "Reference CPU or Device" pattern as part of 10.6's real bug fix (using the established precedent, not inventing a new one) -- P2-1's proper fix (deriving memory class from `PreparedPlan` binding/`KernelAdvertisement.memory_classes`/Provider capability, contractually) remains pre-existing architecture debt this change did not introduce and does not attempt.
- [x] 10.8 Quality/GPU CI on the exact shipped commits. First push after 10.6's work (`0bec882`) turned Quality red on real, previously-undetected gaps local runs missed: `magnetar-cli`'s own bundled `fixtures/qwen-real.component.wasm` is a *separate* copy of the real Component from `magnetar-runtime`'s, left stale by regenerating only the latter (task 10.3), so every `magnetar-cli` test exercising real generation failed closed with `DigestMismatch` against the shared `QWEN_REAL_COMPONENT_DIGEST` constant; and `cargo doc -D warnings` (a Quality job local `cargo test`/`clippy`/`fmt` never runs) rejected three intra-doc-links this task's new public items added, pointing at private items. Both fixed (`e3627c2`): `magnetar-cli`'s copy synced from the same regenerated artifact with its own manifest digest updated: `grep`-confirmed no remaining reference to the old digest anywhere in the repository; the three doc links changed to plain (non-linking) code spans. Quality run `34244779830` on `e3627c2` (the exact final commit) is fully green -- every job, including `docs`, `coverage`, `submodule integration`, `component integration`, and the full `ubuntu`/`macos`/`windows` test matrix. `gpu-runner-smoke.yml` run `34245485600` on the same commit was cancelled by the user after over two hours pending, not failed -- an inconclusive shared-runner-capacity dispatch, matching this project's own established precedent (`implement-cuda-provider-baseline` task 10.3's note) for treating a stuck dispatch as inconclusive rather than a defect; task 10.6's own direct, repeated local verification against this workstation's real RTX 3070 Ti (the same class of evidence `implement-cuda-provider-baseline`/`enable-device-resident-kernel-chaining` already relied on before their own first successful `gpu-runner-smoke.yml` confirmation) already stands as the real-hardware proof this task's GO criterion requires.
