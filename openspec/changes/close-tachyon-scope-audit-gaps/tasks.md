## 1. Real chat template threading

- [x] 1.1 In `loaders/huggingface`, thread the already-parsed `chat_template_reference` (from `tokenizer_config.json`) into the `ModelManifest.chat_template` field produced by `HuggingFaceIngestor::ingest`, instead of hardcoding `None`.
- [x] 1.2 Add a test proving a bundle whose `tokenizer_config.json` declares a `chat_template` produces a manifest carrying it, and a bundle that declares none produces `None`, unchanged from prior behavior.

## 2. Real chat template rendering

- [x] 2.1 Add `minijinja` (or an equivalent minimal, actively maintained Jinja2-compatible crate) as a dependency of `loaders/huggingface` only -- never a `magnetar-runtime` dependency, enforced by the existing `submodule-integration` dependency guard.
- [x] 2.2 Implement a real chat-template renderer in `loaders/huggingface` implementing `magnetar_runtime`'s existing `ChatTemplateFormatter` trait, rendering the artifact's real template against the standard message shape (`role`, `content`) real chat templates expect.
- [x] 2.3 Define and document the supported rendering subset explicitly (variable substitution, `{% for %}` over `messages`, `{% if %}` on loop/message state, the specific filters/tests real checkpoints' templates use). Reject a syntactically invalid template at ingestion time with a structured, named error; a syntactically valid template referencing a construct outside the subset (e.g. an unknown filter, resolved only at call time by `minijinja`) fails closed no later than first render, not silently mis-rendered.
- [x] 2.4 Add tests against at least two materially different real chat template strings (e.g. a Qwen-style `<|im_start|>`/`<|im_end|>` template and a materially different real template from another checkpoint family) proving correct rendering byte-for-byte against the real template's own expected output.
- [x] 2.5 Add a test proving a syntactically invalid template is rejected at ingestion with a structured error, and a test proving a syntactically valid template referencing an unsupported construct fails closed at render time with a structured error naming it, not silently mis-rendered or silently ignored.

## 3. Wiring the real formatter into production generation

- [x] 3.1 Added `run_production_qwen_generation_for_provider_with_prompt` in `magnetar-runtime`, generalizing `run_production_qwen_generation_for_provider` to accept any `PromptInput` plus an optional `chat_formatter: Option<&dyn ChatTemplateFormatter>`; the existing `run_production_qwen_generation`/`_for_provider` signatures are unchanged, now thin wrappers passing `PromptInput::PlainText`/`None` (their exact prior behavior).
- [x] 3.2 (Rescoped after investigation -- see `design.md` Non-Goals: `magnetar-cli`'s chat/run commands are bound only to the separate `qwen-test` fixture and have no existing connection to a production-loaded instance, so there is no CLI chat-formatter selection to extend without building a new, separate "CLI runs generation against a locally loaded production model" feature, which is out of scope here.) Added `real_public_checkpoint_renders_chat_messages_through_its_own_real_template` proving `run_production_qwen_generation_for_provider_with_prompt` renders a real declared chat template for `PromptInput::ChatMessages` end to end against the real public checkpoint; `PromptInput::PlainText` reproduces the pre-existing plain-text behavior unaffected (both pre-existing `real_public_checkpoint_*` tests still pass). Verified on real hardware: the real checkpoint's plain-text path still produces the audit's originally-reported incoherent `" 1000000"`, while the new chat-message path produces a coherent `"The capital of France, France."` for `"What is the capital of France?"` -- concrete, measured proof the gap is closed, not merely that the code compiles. This also surfaced and fixed a second real, adjacent defect the two prior short-plain-text-only tests could not have exposed: `prepare_first_native_plan_for_graph`'s `PlanGuard::SequenceRange` ceiling was hardcoded to the tiny synthetic E2E fixture's own 32-token context (`E2E_FIXTURE_CONTEXT`) for every plan regardless of the real model being served, rejecting any real chat-rendered prompt over 32 tokens with `PlanWorkloadIncompatible`; fixed by scaling the ceiling to `token_count.max(E2E_FIXTURE_CONTEXT)` (the plan's own real built-for size), preserving every existing tiny-fixture test's behavior exactly.
- [x] 3.3 Add a source guard (or extend the existing production-loading source guards) proving `run_production_qwen_generation_for_provider_with_prompt` is reachable through public APIs only, not through Runtime-internal primitives.

## 4. Explicit `Unsupported` signal for a decode shape a Provider cannot perform

- [x] 4.1 Add `fn supports_multi_step_decode(&self) -> bool { true }` as a new default method on the `Provider` trait in `magnetar-runtime`, so every existing implementor's behavior is unchanged unless it explicitly overrides it.
- [x] 4.2 Override `supports_multi_step_decode` to `false` on `CudaProvider` (`providers/cuda`), matching its already-documented, already-tested device-resident-only KV history behavior.
- [x] 4.3 Add a new structured `InferenceApiError::Unsupported { reason }` (or extend an existing structured error enum) naming the real constraint.
- [x] 4.4 Checked in `run_production_qwen_generation_for_provider_with_prompt` (the one place an external caller supplies an arbitrary Provider for production generation; the `qwen-test` fixture path is hardcoded to Reference CPU and never reaches this case) -- before `Runtime::builder()` is even called, when `max_tokens > 1` and `!provider.supports_multi_step_decode()`, fails with the new `Unsupported` error before any real execution work.
- [x] 4.5 Added `multi_step_decode_request_against_an_unsupporting_provider_fails_fast` (`tests_production_loading_e2e.rs`) -- a fake Provider declaring `supports_multi_step_decode() == false`, no registered Component and an untrusted trust store, proving the check runs before any of that could be reached.
- [x] 4.6 Added `multi_step_decode_on_real_cuda_hardware_fails_with_explicit_unsupported` (`tests_production_loading_cuda_e2e.rs`, `#[ignore]`d for `gpu-runner-smoke.yml`) -- verified against real CUDA hardware to fail with `InferenceApiError::Unsupported`, not the previous internal residency error.
- [x] 4.7 Already covered by pre-existing tests: `tachyon_shaped_real_production_ingestion_loads_through_the_real_qwen_component` (Reference CPU, default `max_tokens` 64) and `real_production_ingestion_generates_on_real_cuda_hardware`'s own `max_tokens: 1` case both already exercise a Provider declaring support, unaffected by the new check; both still pass.

## 5. Real `tokens_per_second` measurement

- [x] 5.1 Measured real wall-clock time (`std::time::Instant`) around each prefill/decode step dispatch in `run_generation_loop_inner` (`inference_api.rs`), the one shared loop both `run_generation_loop`/`run_generation_loop_with_execution_plans` call -- covers the production entry points built on it with no Provider-specific code.
- [x] 5.2 Populated `GenerationUsage.prefill_duration_millis`/`decode_duration_millis`/`tokens_per_second` from that real measurement via the new `measured_tokens_per_second` helper, for both Reference CPU and CUDA (verified on real hardware, see below), replacing the always-`None` default when at least one token was produced.
- [x] 5.3 Verified on real hardware rather than a synthetic fixture executor (a trivial fake generation step completes in microseconds, too fast to reliably register a nonzero millisecond duration): extended `tachyon_shaped_real_production_ingestion_loads_through_the_real_qwen_component` (Reference CPU) and `real_production_ingestion_generates_on_real_cuda_hardware` (real CUDA) to assert `tokens_per_second`/`prefill_duration_millis` are `Some` and, on the Reference CPU test, that `tokens_per_second` exactly equals `generated_tokens * 1000 / total_millis`.

## 6. Documentation and scope reconciliation

- [x] 6.1 Recorded in `SUBMODULES.md` (loaders/huggingface row) and README (new "Tachyon scope charter reconciliation" note plus updated "Qwen production loading" supports/not-yet-supported lists) which parts of the Tachyon-authored Magnetar scope charter this change closes, and which remain explicitly deferred exactly as the charter itself frames them.
- [x] 6.2 Updated `docs/production-model-loading-integration.md` with a new numbered recipe step describing the real chat-template behavior for embedders, plus an example.
- [x] 6.3 Documented `Provider::supports_multi_step_decode` (doc comment on the trait method itself, `provider.rs`) and the `Unsupported` error shape (doc comment on `InferenceApiError::Unsupported`, `inference_api.rs`) for external Provider implementers; also covered in `docs/production-model-loading-integration.md`'s "Current profile and limits" section.

## 7. Quality gates

- [x] 7.1 Ran `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings` (the actual `quality.yml` gate; `--no-default-features` is not part of that gate and was found to pre-exist as unclean on `main` even before this change, out of scope to fix here), `cargo doc --no-deps --all-features`, and `cargo test --all-features` for `loaders/huggingface`, `magnetar-runtime` (default features and `--features wasmtime-component-engine`, matching `quality.yml`'s two doc jobs), `providers/cuda` (including real-hardware `--include-ignored` tests on the local RTX 3070 Ti: 33 passed), and `magnetar-cli` -- all clean. `cargo fmt` on `magnetar-runtime` triggered a mechanical, toolchain-forced whole-enum reformat of `InferenceApiError` (adding one doc-commented variant among single-line ones changes rustfmt's collapsing heuristic for the whole enum) -- applied, verified semantically inert (1242 + 173 tests still pass after).
- [x] 7.2 Re-ran the full `integration-tests/production-loading` suite (fast + `--include-ignored` real public checkpoint tests, `--release`), confirming no regression from the chat-template and `Unsupported`-signal changes; this run is what surfaced and confirmed the fix for the `PlanGuard::SequenceRange` defect documented in `design.md`'s addendum.
- [x] 7.3 Ran `gpu-runner-smoke.yml` on commit `39fe220745569027578d4eedaaa128e7c52d1a2c` (run [34681155962](https://github.com/astorise/Magnetar/actions/runs/34681155962), real hardware, 9m35s, all green): confirmed `multi_step_decode_request_against_an_unsupporting_provider_fails_fast`, `multi_step_decode_on_real_cuda_hardware_fails_with_explicit_unsupported` (real CUDA, task 4.6), and `real_public_checkpoint_renders_chat_messages_through_its_own_real_template` (real checkpoint, task 3.2) all genuinely ran (not silently skipped) and passed, alongside every pre-existing real-hardware test.
- [x] 7.4 Evidence, all on commit `39fe220745569027578d4eedaaa128e7c52d1a2c` (`main`): `Quality` run [34681147523](https://github.com/astorise/Magnetar/actions/runs/34681147523) -- every one of its 23 jobs succeeded (rustfmt, clippy, docs, check/test on ubuntu/windows/macos, coverage, msrv, cargo-deny, openspec, submodule/provider/format/component integration, model-family isolation, wasm32/wasmtime component engine, e2e/provider conformance); `GPU Runner Smoke Test` run [34681155962](https://github.com/astorise/Magnetar/actions/runs/34681155962) -- real hardware, all green (see 7.3). `openspec validate close-tachyon-scope-audit-gaps --strict` passes. Archiving now.
