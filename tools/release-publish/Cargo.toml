[package]
name = "release-publish"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false
description = "Credential-free release-proof and OCI manifest tooling for openspec/changes/implement-release-publication-automation (MAG-06)."

[dependencies]
magnetar-roadmap-contracts = { path = "../../roadmap-contracts", version = "0.1.0" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
