mod contract_tests {
    mod support;

    mod affinity;
    mod artifact;
    mod batching;
    mod component;
    mod distribution;
    mod kernel;
    mod kv_cache;
    mod model;
    mod model_instance;
    mod model_loading;
    mod prefix_cache;
    mod provider;
    mod resolution;
    mod runtime;
    mod sampling;
    mod session;
    mod tokenizer;
}
