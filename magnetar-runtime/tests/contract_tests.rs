mod contract_tests {
    mod support;

    mod affinity;
    mod artifact;
    mod component;
    mod distribution;
    mod kv_cache;
    mod model;
    mod model_loading;
    mod prefix_cache;
    mod provider;
    mod resolution;
    mod runtime;
    mod sampling;
    mod session;
    mod tokenizer;
}
